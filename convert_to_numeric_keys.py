import json

numeric = ['count', 'percent', 'year']

convert_to_numbers

with open('nyc-deaths-by-count.json', 'w') as convertable_data:
  #output.write( json.dumps( deaths_by_count, indent=2) )

  output.close()